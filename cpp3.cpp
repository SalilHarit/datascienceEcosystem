#include <iostream>
#include <cmath>

int main() {

    //type conversion = conversion of a value of one data type to another data type
    //implicit conversion = done by compiler
    //explicit conversion = done by programmer

    //implicit conversion
    // int a=10;
    // float b=20.5;
    // float sum=a+b;
    // std::cout<<sum<<std::endl;

    //explicit conversion
    // int a=10;
    // float b=20.5;
    // float sum=a+(int)b;
    // std::cout<<sum<<std::endl;
    
    // std::cout<<(char)100<<std::endl;
    //cout << (insertion operator) = used to insert data into the stream
    //endl = used to insert a new line into the stream
    //cin >> (extraction operator) = used to extract data from the stream
    // std::string name;
    // int age;

    // std::cout<<"Enter your name: ";
    // std::cin>>name;


    // std::cout<<"Enter your age: ";
    // std::cin>>age;
    // std::cout<<"Hello "<<name;
    // std::cout<<"You are"<<age<<"years old";

    double x=3;
    double y=4;
    double z;
    //z=std::max(x,y);
    // z=std::min(x,y);
    // z=pow(x,y);
    // z=sqrt(y);
    // z=round(4.5);
    // z=ceil(4.1);
    // z=floor(4.9);
    // z=fabs(-4.9);
    // z=fmod(5,3);
    // z=log(2);
    // z=log10(2);
    // z=exp(2);
    // z=cos(2);
    // z=sin(2);
    // z=tan(2);
    // z=acos(2);
    // z=asin(2);
    // z=atan(2);
    // z=atan2(2,3);
    // z=cosh(2);
    // z=sinh(2);
    // z=tanh(2);
    // z=acosh(2);
    // z=asinh(2);
    // z=atanh(2);
    // z=erf(2);
    // z=erfc(2);
    // z=tgamma(2);
    // z=j0(2);
    // z=j1(2);
    // z=y0(2);
    // z=y1(2);
    // z=lgamma(2);
    // z=ceil(4.1);
    // z=floor(4.9);
    

    std::cout<<z<<std::endl;
    return 0;
}