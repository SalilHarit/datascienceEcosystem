#include <iostream>

int main(){

    // arithmetic operatrs = +, -, *, /, %
    // assignment operators = =, +=, -=, *=, /=, %=
    // increment/decrement operators = ++, --
    // relational operators = ==, !=, >, <, >=, <=
    // logical operators = &&, ||, !
    // bitwise operators = &, |, ^, ~, <<, >>
    
    int students = 20;
    // students+=1;
    // students-=1; 
    // students--;
    // students=students *2;
    // students*=2;
    // students=students/2;
    // students/=2;
    students=students%2;
    
    std::cout<<students;
}

 